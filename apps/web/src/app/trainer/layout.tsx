import { TrainerLayout } from '../../components/trainer-layout';

export default function TrainerRouteLayout({ children }: { children: React.ReactNode }) {
  return <TrainerLayout>{children}</TrainerLayout>;
}
